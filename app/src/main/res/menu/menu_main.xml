<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/author_fitbit"
        android:title="@string/fitbit_login"
        app:showAsAction="never"/>
    <item android:id="@+id/author_moves"
        android:title="@string/moves_login"
        app:showAsAction="never"/>
    <item android:id="@+id/check_data"
        android:title="@string/check_data"
        app:showAsAction="never"/>
    <item android:id="@+id/reset"
        android:title="Reset Database"
        app:showAsAction="never"/>
</menu>